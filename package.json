{
  "name": "changilweb",
  "version": "1.0.0",
  "description": "Changil Church Website",
  "private": true,
  "scripts": {
    "start": "cd server && npm install && node src/server.js",
    "heroku-postbuild": "cd server && npm install --production"
  },
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  }
}

